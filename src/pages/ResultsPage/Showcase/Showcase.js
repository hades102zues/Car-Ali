import React from "react";
import styles from "./Showcase.module.css";

const showCase = () => {
	return (
		<div className={styles.showCase}>
			<p className={styles.p}>LISTING RESULTS</p>
		</div>
	);
};

export default showCase;
